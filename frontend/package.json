{
  "name": "docintel-ai-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DocIntel AI — React-free-for-now Vite + vanilla JS frontend, talking to the FastAPI backend in ../api. Being replaced page-by-page with the Streamlit UI's feature set (Phases E-J of the migration).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
